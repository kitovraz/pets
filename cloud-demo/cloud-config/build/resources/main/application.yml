server:
  port: 8888
spring:
  application:
    name: sconfig
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configs/

#endpoints:
#  restart:
#    enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka
  instance:
    prefer-ip-address: true


