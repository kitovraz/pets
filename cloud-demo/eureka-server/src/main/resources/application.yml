server:
  port: 9000
spring:
  application:
    name: eserver

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:9000/eureka

logging:
  level:
    com:
      netflix:
        eureka: OFF
        discovery: OFF

